{
  colorschemes = {
    catppuccin = {
      enable = true;

      settings = {
        flavour = "mocha";

        # Needed to keep terminal transparency, if any
        transparent_background = false;
        dim_inactive.enabled = false;
      };
    };
  };
}
